#ifndef CLI_H
#define CLI_H

#define BUFFER_LEN 1024

/**
 * \brief Entry point for parser operation.
 *
 * \param line Holds the contents of the prompt after the function finishes 
 */
void getPromptContent(char **line);

#endif
